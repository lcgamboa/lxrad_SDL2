include ../Makefile.Conf

all:

static:

install:
	${MKDIR} ${includedir}/lxrad_X11
	${CP} -v *.h ${includedir}/lxrad_X11/
	chmod 644 ${includedir}/lxrad_X11/*

uninstall:
	${RM} -R ${includedir}/lxrad_X11

clean:
