include ../Makefile.Conf

all:

static:

install:
	${MKDIR} ${includedir}/lxrad_SDL2
	${CP} -v *.h ${includedir}/lxrad_SDL2/
	chmod 644 ${includedir}/lxrad_SDL2/*

uninstall:
	${RM} -R ${includedir}/lxrad_SDL2

clean:
